<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
    ========================================================================================
    theme
    ========================================================================================
    -->

    <!-- Tio 主题 -->
    <style name="TioTheme" parent="QMUITheme.Blue.NoActionBar">
        <item name="colorPrimary">@color/gray_f3f3f3</item>
        <item name="colorPrimaryDark">@color/gray_f3f3f3</item>
    </style>

    <!-- 欢迎界面主题 -->
    <style name="TioWelcomeTheme" parent="WtTheme.Blue.NoActionBar">
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryDark">@color/white</item>
        <item name="colorAccent">@color/white</item>
        <item name="android:windowBackground">@drawable/ic_launcher_background</item>
    </style>

    <!--
    ========================================================================================
    style
    ========================================================================================
    -->

    <!-- actionBarTheme -->
    <style name="actionBarTheme_black" parent="@style/ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="colorControlNormal">#FF111111</item>
    </style>

    <style name="titleTextAppearance_login" parent="@style/titleTextAppearance_main">
        <item name="android:textSize">28sp</item>
    </style>

    <style name="titleTextAppearance_entrance" parent="@style/titleTextAppearance_main">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">#FF111111</item>
    </style>

    <!-- ThemeOverlay -->
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="android:itemTextAppearance">?android:attr/actionMenuTextAppearance</item>
        <item name="actionMenuTextAppearance">@style/ActionMenuTheme</item>
    </style>

    <!-- toolbar的弹出菜单 -->
    <style name="ActionMenuTheme" parent="Theme.AppCompat.Light">
        <item name="android:textColor">@color/gray_333333</item>
    </style>

    <style name="Toolbar.TitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">16sp</item>
    </style>
</resources>